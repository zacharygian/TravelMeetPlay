<h1>
  Details about the <%= @event.sport.name %> game on <%= @event.nice_date %>
</h1>
<p>This event is organized by: <%= @event.host.first_name %> <%= @event.host.last_name %></p>
<p> This event will take place in <%= @event.address %>. </p>
<p> The host is looking for <%= @event.max_players %> players.</p>
<p> The date set is <%= @event.nice_date %>. </p>
<p> There are <%= @event.spots_left %> spots left.</p>

<% if @event.host.id == current_user.id %>
  <p>This is an event you created.</p>
<% elsif @event.spots_left > 0 %>
  <p>Would you like to register for this event?</p>
  <%= button_to "Register", event_bookings_url(@event), class: "btn btn-primary"%>
<% elsif  %>
  <p>This event is currently full. We will notify you if spots become available.</p>
<% end %>

